# Tech Debt

Tracked follow-ups:
- Add fixture tests for governance dependency-graph violations.
- Add hook install/uninstall idempotence tests against temp config files.
- Add provider hook path and matcher generation tests.
- Add CLI spec/code sync lint after command specs stabilize.
- Add schema boundary validation lint for provider/process JSON parsing.
- Add stricter app-layer import lint fixtures.

Do not use this file to bypass easy boundary fixes. Only record debt when the fix would broaden the current task too much.
