# @tangent/trees-attention Architecture

The attention engine reads typed observations and resource projections, applies deterministic rules, and emits attention items with dedupe keys and source observation IDs.
