# Agent Notes

Purpose: one file per CLI noun: `area.ts`, `goal.ts` (including `goal start` and `goal handover`), `idea.ts`, `agent.ts`, `vault.ts`, and `study.ts` (the partner launcher) with `study-contract.ts` (its system prompt).

Local rules: Follow `../AGENTS.md`. `goal done`/`goal wont-do` run only on the user's explicit word, except a brain started by Julian closing Goals under its own plan on a passing review (ADR-0024).

Read next:
- Package docs/index.md: ../../../docs/index.md
- ../../../docs/public-api.md
