# Agent Notes

Purpose: Shared native provider log schema inspection and compatibility helpers.

Local rules:
- Keep this layer permissive and diagnostic-only.
- Do not normalize native logs into Usage events from this shared layer.
- Do not depend on Rollup, Eval, or Search.

Read next:
- ../../../docs/index.md
