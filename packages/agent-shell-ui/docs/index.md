# @tangent/agent-shell-ui Docs

Purpose: Start and follow Goal-bound Reviewed build Runs in the combined Tangent UI.

The Goals view keeps the common start path to one action. An optional editor changes pending agents, models, effort, and provider sessions.

Read next:
- architecture.md
- public-api.md
