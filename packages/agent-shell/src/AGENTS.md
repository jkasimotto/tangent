# Agent Notes

Purpose: `@tangent/agent-shell` source. `index.ts` re-exports `cli/index.ts`; everything lives under `cli/`.

Local rules: Keep this package independent of browser code. The Agent Shell server and browser interface live in `prototypes/agent-shell/`.

Read next:
- ../docs/index.md
- ../docs/architecture.md
- ../docs/public-api.md
