# Tokens

Tokens are semantic CSS variables exported by `@tangent/ui-tokens`. Themes include light, dark, system, and density modes.

Do not add local product variables such as `--bad`, `--warn`, or `--accent`.
