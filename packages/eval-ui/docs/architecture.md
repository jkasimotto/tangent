# @tangent/eval-ui Architecture

This package renders Eval screens with shared Tangent UI components and consumes `@tangent/eval-ui-data`. It does not own local HTTP routes or server-side file access.
