# Agent Notes

Purpose: @tangent/usage-core core/insights source area. Pure deterministic finding generators, the finding/park-state model, and command-head normalization for the `tangent usage insights` efficiency lens.

Local rules: Pure functions over `NormalizedConversation[]` only; no SQLite, UI, or provider imports. Follow the nearest parent AGENTS.md and the package docs.

Read next:
- ../../../docs/index.md
- ../../../docs/architecture.md
- ../../../docs/public-api.md
