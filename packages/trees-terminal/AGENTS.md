# @tangent/trees-terminal

Purpose: tmux and process runtime adapters for Tangent Trees.

Rules:
- tmux is a runtime, not the product UI.
- Terminal lifecycle actions should produce observations or event-ready resources.

Read next:
- docs/index.md
