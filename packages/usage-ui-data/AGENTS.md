# @tangent/usage-ui-data

Purpose: Usage UI DTOs and adapters, with no React dependency.

Read next:
- docs/index.md

Local rules:
- Map Usage domain data into stable view models.
- Keep React and browser component code out.
