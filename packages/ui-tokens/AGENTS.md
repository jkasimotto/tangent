# @tangent/ui-tokens

Purpose: framework-free semantic design tokens and theme CSS for Tangent UI.

Read next:
- docs/index.md

Local rules:
- Do not import React or product packages.
- Expose semantic tokens only; avoid product-specific color names.
