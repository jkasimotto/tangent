# Agent Notes

Purpose: Svelte UI for browsing and comparing prepared Tangent Eval runs.

Local rules:
- Keep this package UI-only; it talks to `/api/eval/*` and must not import `@tangent/eval`.
- Use shared UI tokens for colors, typography, and diff semantics.

Read next:
- docs/index.md
- docs/architecture.md
- docs/public-api.md
