# Agent Notes

Purpose: Local HTTP server for the Usage UI and `/api/usage/*` routes.

Local rules: Keep server code a thin adapter over public Usage core APIs and UI-data DTOs. Do not put React or browser-only code here.

Read next:
- ../../docs/index.md
- ../../docs/architecture.md
- ../../docs/public-api.md
