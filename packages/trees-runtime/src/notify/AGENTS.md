# @tangent/trees-runtime src

Purpose: Per-run agent notify watcher. Polls a started agent's terminal pane and fires a desktop notification when it finishes or needs input.

Read next: ../docs/index.md.
