# Critique

## Verified claims

Checkout HEAD `488cc0b` equals the live server's `deployedCommit` (`GET /api/sessions` on 127.0.0.1:4321, checked 2026-08-27), so line numbers below are against the running code. Working tree is clean apart from three untracked docs (`docs/design/agent-shell-navigation-model/`, `docs/design/agent-shell-operating-vision/`, `handover.md`).

1. **Worker cwd is a silent three-way fallback** (worker-cwd, area-skills). CONFIRMED. `packages/agent-shell/app/server.mjs:2339` `const dir = workingDirectory || (await areaDirectory(area)) || path.join(TREES_ROOT, area);` then `createOwnedTmuxSession(phaseName, ["-d","-s",phaseName,"-c",dir])` at `:2342`. `areaDirectory` (`:466-471`) reads only `<area>/<leaf>.md` via `areaResource` (`:449-458`), no ancestor walk.

2. **Brain cwd uses the same resolver** (worker-cwd, area-skills). CONFIRMED. `server.mjs:4804` `const directory = (await areaDirectory(record.area)) ?? path.join(TREES_ROOT, record.area);`, tmux at `:4839`.

3. **A start with no `--launch` from a non-brain caller is refused 400; ADR-0035 says Tangent supplies no harness from an Area note** (quick-goal). CONFIRMED. `server.mjs:2640-2648` `missingStepLaunches` builds `step N has no --launch. Every step names its own harness...`; `materializeStepLaunches` (`:2711-2735`) fills `launchSource: "brain-default"` only when `brainWorkerLaunch(brain)` is non-null; comment at `:2708-2710`. `brain-worker-launch-http.test.mjs:293-299` asserts 400 matching `/step 1 has no --launch/`. `docs/decisions/ADR-0035-worker-harness-comes-from-the-brain.md:21`: "Tangent still supplies no harness from a profile, from an Area note, or from a recorded command." ADR committed `478de65` at 2026-08-27 01:23 (+1000).

4. **A worker handover on a v2 queue never starts the next assignment** (send-not-handover, quick-goal). CONFIRMED. `server.mjs:3252-3253`: `if (record.schema === "area-goal-queue.v2" && ["agent","skip"].includes(source)) { if (source === "agent") return workerResponse("reported", { index: next.index, session: null });`. With nothing pending, `:3245-3251` return `workerResponse("reported")`; `goal-done` only in the `typed?.closeGoal` branch `:3229-3243`.

5. **Only a designated `review-result` under `completionPolicy === "review-pass"` computes `closeGoal`** (requests, quick-goal, send). CONFIRMED. `packages/agent-shell/app/area-brain-domain.mjs:411-418`. Allowed report types at `:383`; assignment status rule at `:405`.

6. **`routeBrainNotice` writes the inbox first, then logs `kept` when no record or no live brain, and never starts a brain** (all themes). CONFIRMED. `server.mjs:4231-4262`: `recordBrainNotice` at `:4238`, `kept ... "Area brain has not started yet"` at `:4243`, `kept ... "no live brain; waits for the next generation"` at `:4248`. `exactLiveBrainForArea` (`:4121-4127`) requires `status === "active"`, a live session, and `instanceId === INSTANCE_ID`.

7. **Only a live brain can create a Request** (requests). CONFIRMED. `server.mjs:6099-6101` `const brain = await liveBrainForSession(session); if (!brain) return { status: 403, error: "only a live brain can create a request" };`. `REQUEST_KINDS` and `REQUEST_EFFECT_KINDS` at `brain-requests.mjs:7-8`.

8. **Describe work routes to the brain whenever any brain record exists, and resumes an inactive brain** (quick-goal, send). CONFIRMED. `server.mjs:6703-6713` (`brainRecordForArea` any status -> `describeWorkToBrain`); `describeWorkToBrain` `:4349` `startBrain(owner.area, { resume: true, messageRecorded: true })`.

9. **`continueWorkerSession` passes `path: assignment.path` at `server.mjs:3903`** (worker-cwd Observed 3 and counterexample "Continuation must carry the directory"). REFUTED. No `continueWorkerSession` exists in `server.mjs`; `session-safety.test.mjs:33-36` asserts `assert.doesNotMatch(source, /continueWorkerSession|continueWorker:/)` and `governance/src/index.ts:128` lints `operations.continueWorker`. The `path: assignment.path` line at `:3903` is inside `replaceGoalAttemptUnlocked` (`:3831`, `replacementAttemptId` at `:3884`), the Change-agent replacement path. The substantive point survives with the right name: the replacement path is the third spawn call site that must carry the directory. The send-not-handover theme's claim that worker self-replacement is gone is the correct one.

10. **The speedrun trigger session sits in the vault Area folder "consistent with a vault-folder fallback"** (worker-cwd Observed 5). REFUTED as to cause. `~/.tangent/trees/neara/pgande/speedrun/.processes.json` sets `"cwd": "/Users/julianotto/.tangent/trees/neara/pgande/speedrun"` explicitly, and `launchTriggerAgent` uses `definition.cwd` (`src/cli/triggers.ts:287`). The processes theme ("cwd is the Area folder itself") is right. The trigger runtime never falls back to the vault folder by itself; `parseTriggerManifest` derives cwd from the note's Repository/Worktree when the entry has none.

11. **`brain-command-reference.mjs` has no non-test caller** (send, area-skills, processes). CONFIRMED. `grep -rl` over `packages/agent-shell/app` and `src` returns only `brain-command-reference.test.mjs`. `brainPrompt` required sections at `server.mjs:4592` are `Identity, Boundary, Execution contract, Wake, Work frontier, Questions, Unread messages, Asking Julian, Retrieval order`; it also carries an optional `Recent milestones` section (`:4589`) no theme mentions.

12. **Pacing counts any successful POST naming the session as "acted", keyed on route path** (send). CONFIRMED. `server.mjs:7149` `if (url.pathname !== "/api/brains/handover") brainPacing.noteAction(actingSession(req.parsedBody));`.

13. **The browser fills the Area Work default silently on the wire** (quick-goal). CONFIRMED. `public/goal-launch-view.js:393-396` `launchStepRequest`: no command, no choice -> `options.default` becomes `launch` (or `command`). The Launch Editor start posts no `caller`.

14. **No brain is live; 16 Areas hold unread notices** (quick-goal, processes). CONFIRMED. All 16 `brain.json` records read `inactive|ended|stopped` (otto/tangent inactive gen 324; neara inactive 189; otto/launcher stopped 33). 16 `inbox.json` files have `deliveredAt: null` notices. `neara/pgande/speedrun` has an inbox but no `brain.json`.

15. **"Agent Shell has no URL routing"** (requests Gap). PARTLY REFUTED. No `location.hash`, `pushState`, or `hashchange` in `public/*.js` (confirmed), but `public/shell-state.js:12` reads `new URL(href).searchParams`, and the navigation-model record (`docs/design/agent-shell-navigation-model/design-record.md:174`) states "URLs stay as deep-link entry points only (`?view=`, `?document=`, extended cheaply to `?goal=` if wanted)". A `?request=` or `?goal=` entry point is the sanctioned shape, not a hash route.

16. **Worker session name is `<areaLeaf>--<slug>[-sN]`** (quick-goal) vs `<area>-<goal>-sN` (requests). BOTH PARTLY RIGHT. Source `server.mjs:2602` builds `normName(`${leaf}--${slug}`)` but live names are single-dash (`tangent-finish-and-prove-tangent-around-the-area-brain-s7`), so `normName` collapses the double dash; trigger names keep it (`trigger-speedrun--speedrun-pgande-7e780adf`, `triggers.ts:120-122`).

17. **`tangent goal reopen` exists** (quick-goal Candidate C relies on it). CONFIRMED. `src/cli/commands/goal.ts:33`, `spec.ts:276-280` ("Return a done, parked, or won't-do Goal to open without starting an agent"). `park` at `spec.ts:270` and `replace-agent` at `:282` also exist.

18. **`tangent harness list --area otto/tangent` prints work and brain defaults** (quick-goal). CONFIRMED by running it: `work default: claude-otto/opus-5/medium ... claude-otto --model claude-opus-5 --effort medium`, `brain default: codex/sol/low ... codex --model gpt-5.6-sol -c model_reasoning_effort=low`.

## Missing

## 1. Julian's memo transcript is on disk and only one theme read it
`docs/design/agent-shell-operating-vision/user-intent.md` (untracked, dated 2026-08-27) holds all three memos verbatim plus an eight-point summary. Six themes list "Julian's exact words" as Unknown. Facts it settles:
- Memo 1 on quick goals: "I don't need to go through the brain. I can just choose the area, choose the harness, let it rip. And when it's done, maybe it can send the message to the brain." So the brain notification is optional ("maybe"), and the inputs are Area and harness only; no done condition is mentioned.
- Memo 1 on send: "we can all just make it send. It's all just agent communication." Nothing about Tangent's audit notices or the generation swap.
- Memo 1 on processes: "I'm happy to have an agent do it for me. But I need to be able to read them and very clearly see what sort of processes I have, when they trigger, what their trigger [conditions] are." Read-only view is explicit.
- Memo 1 on harnesses: token context is "not super useful"; "more importantly ... resume conversations ... the command to resume the conversation gets put onto like the goal note or something". So the priority is the resume command, and "goal note or something" leaves a projection acceptable.
- Memo 1 principle: "things that I'm not going to be changing all the time, I'm happy to have as like read-only views and I can just ask a brain to change [them]."
- Memo 2: "if we take the .agents folder and we also create a .claude folder and we [symlink] the .claude to the .agents within each area, then every brain will be aware of that, because the brains should open in the tangent repository." and "the workers should never really be spawned in the tangent repository. The worker should be spawned in the work repository."
- Memo 3: "I will say if I want to validate or verify a feature ... a clean, like, consistent user interface that's consistent with other notification systems out there, that kind of alerts me and respects the focus system as well."
- Framing: "The most recent work has been very good and steps in the correct direction." Julian asked for a design up front, then agreement before implementation.

## 2. Three accepted ADRs from 2026-08-27 no theme cites
- `ADR-0036-agent-shell-process-ownership.md`: every session Agent Shell creates carries `@tangent_agent_shell_instance`; creation and termination use tmux's immutable session id. Trigger and process sessions created by the root CLI (`src/cli/triggers.ts:287-289`, `src/cli/processes.ts:190-193`) set `@tangent_kind`, `@tangent_area`, `@tangent_process` but no instance option, so they sit outside the ownership domain. Any "Resume a dead attempt" (harness-resume) or server-run process (processes) must create sessions through `createOwnedTmuxSession`.
- `ADR-0037-brain-attempt-launch-override.md`: a user start or resume may pass one registered `{ harness, model?, effort? }` for a brain attempt; raw commands are invalid "because an unqualified worker can inherit that identity under ADR-0035". This is the precedent for a per-attempt harness picker the quick-goal surface would reuse (`brain-launch.mjs:8 resolveBrainAttemptLaunch`).
- `ADR-0038-agent-shell-keyboard-ownership.md`: one visible surface owns each key; a new quick-create surface or a Resume verb must register in `public/work-commands.js` and `keyboard-context.js`.

## 3. The navigation-model design record and `handover.md` (same day, untracked)
`docs/design/agent-shell-navigation-model/design-record.md` decides an object tree `Area > Brain / Goal (recursive) > Assignment > Attempt` plus `Definition (work-definition session)`, Programs, Documents (`:100-117`); "Restart an agent = replace-attempt (the domain's only restart); change harness = same verb, different launch" (`handover.md`); Go To indexes assignments and live sessions; URL router rejected (`:174`). The harness-resume Resume verb belongs on the Attempt row of that tree, and the requests theme's click target is `?goal=`/`?request=` entry, not a hash route. The handover also records that decisions 28-42 (h/l walk, Park status, Goal reader, pending-assignment editor, atomic Change agent) are now committed (`2686f8f`, `3b7736a`, `52f972e`).

## 4. Journal capture is the existing "message that wakes an inactive brain"
`packages/agent-shell/app/voice-routes.mjs` (`POST /api/voice` with Groq transcription, `POST /api/command` typed) and `area-routes.mjs:11` `POST /api/areas/journal` call `operations.capture` (`server.mjs:6355-6372`): Journal entry committed to the vault first, milestone `kind: "journal"`, then `deliverJournalToBrain` (`server.mjs:4435-4444`): `notifyBrain`, and if a `brain.json` exists but nothing is live, `startBrain(area, { resume: true, messageRecorded: true })`; returns `brain-opened | no-brain | not-started | brain-started | brain-resumed`. ADR-0033 implementation notes: "A capture the vault refuses records no milestone and wakes no brain ... Exactly-once covers the whole Journal." Quick-goal Candidate C and processes Candidate A both re-invent this wake; it already exists with idempotency and a route word for the surface.

## 5. Milestones
`brains/<area>/milestones.json`, schema `area-milestones.v1`, `items[]` with `kind` in `goal-done`, `journal`, `routed-journal`, `operation-problem-opened|resolved`, `declared-result`; `appendMilestone` has 7 call sites in `server.mjs`; `GET /api/areas/milestones` (`area-routes.mjs:9`); the brain prompt has an optional `## Recent milestones` section (`server.mjs:4589`). ADR-0033: "Every Goal closure funnels through one milestone record." A quick Goal that closes, a process fire, and a verified Goal should all be milestones; none of the themes name the record.

## 6. Goal dependencies
`tangent goal depend <slug> --on <prereq>` / `undepend` (`spec.ts:178-193`), `POST /api/goals/depend|undepend` (`goal-query-routes.mjs:10-11`), `goal-dependencies.mjs`, and `goal-detail.mjs:74-83` disables `start` with "This Goal has unfinished or missing prerequisites." A quick start on a dependent Goal is refused by the reader's command model even though `startPipeline` does not check dependencies (unverified: whether `startPipeline` enforces `dependencies.blocked`). A Goal-creating process could chain fires by dependency.

## 7. Park, replacement, and the runtime records around attempts
`goal-execution-transition.mjs`: `parkCurrentGoalAttempt` sets `attempt.disposition = { type: "parked" }`, `assignment.status = "stopped"`, `queue.status = "parked"`, `queue.parks[]`; `reopenParkedGoalQueue`; `promoteReadyReplacement`; `attachLateSourceEvidence`. `goal-attempt-replacement.mjs` schema `goal-attempt-replacement.v1` (two-phase, operation id, `--confirm`), runtime dirs `~/.tangent/agent-shell/goal-cleanups/` (`goal-cleanup.v1`) and `armed/` (`armed-prompt.v1`, `armed-prompts.mjs`). Harness-resume's "dead tmux session: create a new owned session and bind it as a new attempt" is exactly `replaceGoalAttemptUnlocked` with a different launch; it should not be a fourth spawn path.

## 8. A second Goal-creation client
`packages/agent-shell/app/goal-command.mjs` (`goalCreateRequest`, `runGoalCommand`, allowlisted options, posts `/api/goals/create`), referenced in `src/cli/client.ts:4` as the local-server contract, and in the Area note idea (`tangent.md:64`): "agents that interact with Tangent should use deterministic tools (commands like goal-command) for every mutation". Quick-goal's composite must not become a third.

## 9. Root CLI spec is stale and has no `send` noun
`src/cli/index.ts:68-78`: `brain` is described as `<handover|status|stop>` (the package dispatches `request|advance|withdraw` too, `commands/brain.ts:13-19`); `goal` as `<create|list|show|start|handover|done|wont-do>` (package adds `own|release|append|park|reopen|replace-agent`). Top-level nouns: `process`, `trigger`, `agent`, `area`, `brain`, `shell`, `goal`, `harness`, `handover`, `idea`, `document`, `study`, `vault`, `governance`. No `send`, so `tangent send` is free (resolves the send theme's Unknown). `tangent handover` is the root-level worker verb (`:74`); `~/.agents/AGENTS.md` teaches `tangent goal handover` instead, and `packages/agent-shell/AGENTS.md:7` says workers use "top-level `tangent handover`".

## 10. A third harness-selection mechanism: the `- Agent:` resource line
`src/cli/triggers.ts:302-314` `inheritedAgentCommand` walks ancestors for `- Agent: <command>` under `## Resources` and falls back to `claude-otto` (under `otto/`) or `claude`; `speedrun.md` declares `- Agent: pi-code --provider resetdata-glm --model zai/glm-5.2 --thinking medium`. `server.mjs:446` doc comment still names `- Agent: claude` as a per-Area setting. This coexists with `tangent.environment.v1` (`launch-environment.mjs:236-250`, "never substitutes a command of its own") and contradicts ADR-0035's spirit for one class of worker. The processes theme observed it; quick-goal and harness-resume treat the registry as the only source.

## 11. pi trust store
`~/.pi/agent/trust.json` is `{"/Users/julianotto/Projects": true}`. The vault is not trusted, so a pi brain started in `~/.tangent/trees/<area>` with `.agents/skills` present would prompt (area-skills Unknown resolved).

## 12. Governance lints that fence several candidates
`packages/governance/src/index.ts:128-150`: forbid `operations.continueWorker`, `body.continue =`, a `continue` CLI option; forbid `newContinuationRecord|writeContinuation|soloExecution`; forbid `row.kind === "test" ... cascadeGoalDone`; forbid the For-you strip, Dock badge (`setAppBadge|clearAppBadge|data-enable-dock-badge`), inferred asks, and `./ask-core.js` imports in Work files. Requests Candidate (a)'s notification and (d)'s synthetic review, and send Candidate D, touch these.

## 13. Vault README rules that are stale or binding
`~/.tangent/trees/README.md:21` frontmatter allowlist "and no others" (`type`, `status`, `due`, `owners`, `waiting_on`), `:79-80` `recur-<slug>.md` routines (deleted by ADR-0029), `:91` "`threads.md` is generated by `tangent threads sweep`" (deleted), `:92` "No other tooling for the vault: no CLI, no schemas, no databases" (contradicted by ADR-0020 and the `tangent.environment.v1` / `tangent.harnesses.v1` blocks). Harness-resume B, requests `verify:`, processes A, and area-skills B all need one README amendment.

## 14. Workspace vocabulary
`packages/agent-shell/app/workspace/AGENTS.md` (the chat pane's instructions) defines "A Program is a repeatable operation attached to an Area" and "A Run is one agent session". The processes theme's naming decision must include this file and the nav tree's "Definition" row.

## 15. How a finished quick Goal renders today
`desk-projection.mjs:31-34` `goalAttention`: `waiting` if `goal.waitingOn` or session `waiting|shell`, else `working` with a session, else `ready`. `public/work-desk-view.js:1256-1260` `idleGoalState`: "Ready for validation" only with `goal.status === "ready"` or an open Test; "Preparing validation" when `goalRunEnded(goal)`; else "Open". So a quick worker that reported `complete` and died leaves a Goal reading "Preparing validation" indefinitely (quick-goal Unknown resolved).

## 16. Message log volume since 2026-08-26
`~/.tangent/agent-shell-messages.jsonl` (3.4 MB): `sent` 400, `delivered` 261, `dropped` 75, `kept` 54, `work mutation` 34, `not delivered` 2. `message-queue.json` absent (nothing pending).

## 17. Area note ideas that bear on the themes
`~/.tangent/trees/otto/tangent/tangent.md:65` "task deferral ... capture a task without acting on it ... recalled on demand" (quick-goal and requests parking); `:86-87` dead or quota-walled steps sit silent 10 minutes (quick-goal idle notices); `:94` a waiting brain still gets a fresh generation every turn (pacing background); `:33` the `~/.claude-otto/skills` symlink drift incident.

## Contradictions

1. **`continueWorkerSession` exists (worker-cwd) vs is absent (send-not-handover).** Absent. `server.mjs` has no such function; `session-safety.test.mjs:33-36` and the governance lint forbid it. The `path: assignment.path` spawn at `server.mjs:3903` belongs to `replaceGoalAttemptUnlocked` (`:3831`). Worker-cwd's list of spawn call sites should read: `startPipelineStep` (`:2833`), `replaceGoalAttemptUnlocked` (`:3903`), solo start, Describe work (`:2237`), brain (`:4804`).

2. **Trigger session cwd: "vault-folder fallback" (worker-cwd) vs "cwd is the Area folder itself" (processes).** Explicit. `.processes.json` sets `cwd` to the Area folder; `launchTriggerAgent` uses `definition.cwd` (`triggers.ts:287`). No fallback ran.

3. **Brain cwd target: area-skills Candidate A moves brains to the vault Area folder; worker-cwd D1 proposes `repoRoot` (`~/Projects/otto-tangent`) and D2 `TREES_ROOT`.** Memo 2 ties "tangent repository" to per-Area `.agents` folders "within each area", which live under `~/.tangent/trees/<area>`. Harness discovery is cwd-anchored and stops at the git root of the cwd, so only a cwd inside the vault (D2 or the Area folder) satisfies memo 2; D1 satisfies the literal words and fails the intent. Resolve to a vault-based constant (Area folder for native discovery, or `TREES_ROOT` plus prompt-listed skill paths).

4. **"Tangent never picks a harness" (quick-goal, harness-resume, ADR-0035) vs trigger workers get `- Agent:` with a machine fallback (`triggers.ts:314`, `claude-otto`/`claude`).** Both true for different executors. ADR-0035 governs the Goal queue; the root trigger runtime predates it (ADR-0030) and ADR-0034 only redirected trigger events to the inbox. A Goal-creating process that starts a worker through `POST /api/goals/start` inherits the ADR-0035 refusal, so the processes design must name a harness source (Area work default via an explicit sentinel, or the `- Agent:` line retired).

5. **"No URL routing" (requests) vs "`?view=`, `?document=` deep-link entry points" (navigation record `:174`, `shell-state.js:12`).** Query-parameter entry points exist; hash and history routing do not and were rejected. A notification click target is `http://127.0.0.1:4321/?goal=<file>` style, added "cheaply".

6. **Notices "sit unread until Julian starts or resumes a brain with an instruction" (quick-goal) vs "only Journal wakes" (processes) vs "Describe work resumes" (send, quick-goal).** Three wake paths exist and none is `routeBrainNotice`: `describeWorkToBrain` (`server.mjs:4349`), `deliverJournalToBrain` (`:4441`), and the reconcile pace wake for a live paused brain. All go through `startBrainUnlocked(area, { resume, messageRecorded })` (`:4942`), which refuses an inactive start with no instruction unless `messageRecorded` or `automaticRecovery` (`:4989`). Any "tell the brain when done" wake is one more caller of that same function.

7. **Session name separators (quick-goal `--` vs requests `-`).** Source builds `leaf--slug` (`server.mjs:2602`), `normName` collapses it; observed queue sessions use one dash, trigger sessions keep two (`triggers.ts:120-122`).

8. **`POST /api/goals/create` line numbers (quick-goal 6981-7009 vs processes 6979-7000).** `async create(body)` starts at `server.mjs:6980` after its doc comment at `:6979`; `createSimple` at `:6964-6978`. Both digests describe the same handlers.

9. **Harness-resume says Usage's index is a viable read-time source (Candidate C) while worker-cwd observes the Usage SQLite index is stale (max `last_activity_at` 2026-08-16) and most Claude sessions have `repo: {}`.** Worker-cwd's observation stands and strengthens harness-resume's own rejection of C: the index lags days and `package-boundaries.md:55` forbids the import.

10. **Requests: "Tests without a Goal exist" (neara 7 of 16) vs quick-goal: `--goal` is a slug lookup (`brain.ts:51-69 requireGoal`).** Both true: `--goal` is optional on `tangent brain request`; `brain-request-mechanics.md` memory records that a Test without `--goal` can never close its Goal. Any verify design must make the Goal link mandatory for a Test.

11. **Area-skills: "`~/.pi/agent/trust.json` contents unknown" vs processes: the speedrun trigger runs pi-code in the vault folder today without a trust prompt.** `trust.json` trusts only `/Users/julianotto/Projects`. The speedrun folder has no `.agents/skills`, and pi prompts only when project skills are present (`pi-mono README:298`), so both observations hold; adding `.agents/skills` to a vault Area would introduce the prompt.

## Cross-cutting

1. **One harness-source decision feeds four themes.** Quick-goal (a human start with no `--launch`), processes (a fired process that starts a worker), area-skills/worker-cwd (brain harness lending across `--path` steps), and harness-resume (the resume command must keep the registry command word, `claude-otto` vs `claude`). Today three sources exist: `tangent.environment.v1` inheritance (`launch-environment.mjs:236-250`), the brain's lent `resolvedLaunch` (ADR-0035, `server.mjs:2698-2735`), and the `- Agent:` note line with machine fallback (`triggers.ts:302-314`). ADR-0037 already allows a per-attempt registered choice for brains. A single ADR amendment (explicit `default` sentinel disclosed as `launchSource: "area-work-default"`, `- Agent:` retired) resolves all four; the `launchDisclosure` row (`server.mjs:2750-2767`) is the record that must show the source.

2. **Skills require a vault cwd for brains; the worker invariant requires a repository cwd for workers; processes need a cwd for probes and Goal workers.** All three read `areaDirectory` (`server.mjs:466`) or `programDirectory` (`programs.mjs:44`). Worker-cwd Candidate A (inherited binding, refusal, recorded `cwd` on the attempt) is a precondition for: area-skills (workers in foreign repos can only reach skills by absolute path in `## Sources` or the instruction), processes Candidate A (`action: goal` needs a directory the worker can be refused for before any record is written, same contract as `missingStepLaunches`), and harness-resume (the resume command needs the attempt's exact cwd because Claude and pi key sessions by cwd; today no record holds it, `grep '"cwd"'` over 285 records is 0).

3. **"Send" is the transport every other theme's notification rides.** Quick-goal "tell the brain when done" is `workerHandoverNotice` -> `routeBrainNotice`; processes' `declared-result`/problem events are `notifyBrain` (`server.mjs:6399-6421`); requests' answers are `brainRequestAnswerNotice` -> `notifyBrain` (`:6155`); `recordCommittedCommand` audit lines (`:4275-4299`) reach the same inbox. Whether the inbox and `message-queue.json` merge (send Candidate B) decides durability for all of them. The wake policy (call `startBrainUnlocked` with `messageRecorded`, paced by `brain-pacing.mjs`) is likewise shared: quick-goal Candidate C, processes' brain hand-off, and Journal capture must use one rule or otto/tangent's 324 generations grow with every quick task and process fire.

4. **`completionPolicy` is claimed by two themes.** Quick-goal Candidate C adds `"worker-report"`; requests Candidates (a)/(b) add `"julian-verify"`; today only `"review-pass"` exists and no caller sets it (`area-brain-domain.mjs:328,416`, `pipeline-record.mjs:114,128,146`). These must be one enum written at `newPipeline` (`server.mjs:2893,2914`) with one closure branch in `completePipelineStep` (`:3229-3243`) and one milestone kind, or the two designs produce two ways to close a Goal.

5. **Quick Goal and process-created Goal are the same composite.** Processes Candidate A's `action: goal` (create with `caller: process:<area>:<name>`, dedupe, then start) is quick-goal Candidate A's `POST /api/goals/quick` with a machine caller. Both need: an optional or defaulted done condition (vault rule at `README:Outcomes`; enforced at `server.mjs:6969` and `:6983`), slug dedupe beyond `allocateGoalSlug -2/-3` (`:1213-1220`), the harness source from item 1, and an origin field on the Goal (frontmatter allowlist, `README:21`). Designing them separately would create two routes for one operation; `goal-command.mjs` is already a second creation client.

6. **Skill chains, pipelines, and process bodies converge on one Markdown-to-steps reader.** Area-skills Candidate D (`tangent.pipeline.v1` block -> `steps` for `POST /api/goals/start`), processes Candidate A (process file body -> Goal description or instructions), and the existing trigger `instructions` file (`triggers.ts:278`) are the same lane: a committed vault Document that becomes worker input. One fenced-block convention (precedent: `tangent.environment.v1`, `tangent.harnesses.v1`) should serve both, and `sourceDocuments` (`server.mjs:1246-1260`, `kind === "document"`) already carries such Documents into `## Sources`.

7. **Attempt identity is the join key for resume, replacement, park, and instance ownership.** Harness-resume must store the provider session on the attempt (`attempts[].id`, not the reused tmux name), the Resume verb must run through `replaceGoalAttemptUnlocked` (two-phase `goal-attempt-replacement.v1`, immutable tmux target per ADR-0036), park/reopen (`goal-execution-transition.mjs`) mark attempt dispositions the resume surface must respect, and the navigation model puts the Resume verb on the Attempt row. Processes' retained REPL sessions have no attempt record at all (`triggers.ts:282-289`), so they can never be resumed or owned until they become queue attempts.

8. **Brain prompt budget is shared.** `BRAIN_STRUCTURAL_LIMIT = 6_900` with required sections (`server.mjs:4592`); optional sections already include Area memory, Selected Documents, Recent milestones. A `## Skills` list (area-skills B), a `## Processes` line (processes), and more inbox notices per quick task (quick-goal) compete for the same budget and drop silently into `## Omissions`.

9. **Every new or renamed command is learned only through `~/.agents/AGENTS.md`.** The brain prompt carries no command manual (`brain-command-reference.mjs` uncalled), workers learn `tangent handover` from `pipelineStepPrompt` (`server.mjs:1770-1776`), and the root CLI spec descriptions are stale (`src/cli/index.ts:70,72`). `tangent send`, `goal quick`/`--start`, `process define`, `--verify`, `--skill`, and any `--launch default` sentinel each need an AGENTS.md bullet, a spec entry, and reminder-text rewrites (`server.mjs:5432`, `:5277`, `:5050-5056`, `:5493-5495`) in the same commit, because live lineages are primed with the old text.

10. **The vault README is a shared dependency.** Frontmatter allowlist (`:21`), "no conversation IDs", "no CLI, no schemas" (`:92`), the stale `recur-` rule (`:79-80`), and the `.processes.json` ignore rule (`.gitignore:8`) constrain harness-resume B, requests' `verify:`, processes A, and area-skills A/B. One README revision should accompany the unified design, not five.

11. **Naming collisions across themes.** "process" (servers/watchers in `tangent process`, AGENTS.md, README `:69-77`, `@tangent_process` tmux option shared with triggers) vs Julian's processes; "Program/Operation/Run" in `workspace/AGENTS.md`; "Definition" (work-definition session) in the navigation tree; "resume" (reattach a live tmux session, wake a brain, `POST /api/brains/start { resume: true }`) vs harness conversation resume; "Focus" (macOS Focus in memo 3, Area Focus in ADR-0027 amendment, deleted focus concept in memory). The unified design needs one glossary before any CLI noun is chosen.

12. **Governance lints and tests pin the removals the candidates lean on.** `session-safety.test.mjs`, `context-continuation.test.mjs`, `brain-worker-launch-http.test.mjs:293-299`, and `governance/src/index.ts:128-150` will fail on: a `--launch default` that bypasses the 400 without disclosure, any worker self-resume path, a synthetic review closure keyed on `row.kind === "test"`, or a Dock badge. Each candidate that touches them must list the lint or test it amends.
