# Captures

Capture replaces the old inbox item.

Captures are structured scratch inputs with kind, text, source, creator, status, resolution, and evidence. They are not the project-memory system, but future rollups may consume them.
