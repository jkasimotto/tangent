# @tangent/eval-ui-data Architecture

This package prevents React components from becoming Eval data mappers. It defines run, spec, variant, compare, and job view models and a browser/server adapter contract.
