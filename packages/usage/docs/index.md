# @tangent/usage Docs

Purpose: Conversation telemetry domain: schemas, native transcript normalization, hook normalization, native-log compatibility checks, datasets, SDK, human `usage` CLI, and explicit raw/debug subcommands.

Read next:
- architecture.md
- public-api.md

Package rules:
- Do not depend on Daily, Eval, or Search.
- Do not own provider hook config mechanics.
