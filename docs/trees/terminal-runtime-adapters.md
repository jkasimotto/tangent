# Terminal Runtime Adapters

Runtime adapters implement create, start, attach, capture, send, kill, list, and optional observation streaming.

V1 includes tmux and process runtimes. tmux is durable runtime infrastructure, not the primary UI.
