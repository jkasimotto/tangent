# @tangent/ui-server

Purpose: reusable local-only HTTP server for Tangent UI assets and API routes.

Read next:
- docs/index.md

Local rules:
- Keep server APIs framework-agnostic.
- Do not embed product UI strings.
