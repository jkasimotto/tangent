# @tangent/trees-store-sqlite

Purpose: optional SQLite projection boundary for Tangent Trees.

Rules:
- Do not make SQLite required by `trees-core`.
- Keep native driver imports behind this optional package.

Read next:
- docs/index.md
