# Agent Notes

Purpose: @tangent/eval-ui browser source area.

Local rules:
- Keep API types serializable and local to the UI/client boundary.
- Do not import `@tangent/eval`; use `/api/eval/*`.

Read next:
- ../docs/index.md
- ../docs/architecture.md
- ../docs/public-api.md
