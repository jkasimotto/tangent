# Attention Model

Attention is generated, not manually authored.

Rules cover permission requests, agent done/failed/quiet states, exceeded estimates, dirty worktrees without checkpoints, unresolved captures, and review states.

Each item has a dedupe key, severity, priority, source observation IDs, evidence, and actions.
