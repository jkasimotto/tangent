# @tangent/trees-runtime

Purpose: Runtime adapters for Tangent Trees storage, git worktrees, terminals, agent commands, attention rules, and optional projections.

Local rules:
- Keep schema contracts in @tangent/trees-schema.
- Keep pure event/projection APIs in @tangent/trees-core.
- Do not import Trees UI packages here.

Read next: docs/index.md.
