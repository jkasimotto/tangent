# @tangent/trees-git Architecture

The Git package attaches entities to registered projects and ensures worktrees using Tangent repo helpers. It emits event-ready results for core instead of becoming a canonical store.
