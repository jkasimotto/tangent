# @tangent/trees-runtime Architecture

`@tangent/trees-runtime` groups runtime adapters that are not stable enough to publish as separate packages. It exposes subpaths for filesystem storage, optional SQLite projection placeholders, git worktree helpers, terminal runtimes, agent command adapters, and deterministic attention rules.
