# @tangent/trees-server

Purpose: local HTTP command-center server for Tangent Trees.

Rules:
- Bind to `127.0.0.1` by default.
- Protect local UI/API access with a random token.

Read next:
- docs/index.md
