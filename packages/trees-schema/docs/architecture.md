# @tangent/trees-schema Architecture

This package owns TypeScript resource types, event contracts, confidence/source models, and lightweight validators. It intentionally has no Node-only integration code.

Forbidden dependencies: React, SQLite, tmux/process adapters, Git adapters, and old `pa` code.
