# @tangent/ui-charts Architecture

Charts receive serializable view models and render token-driven SVG/HTML views. They never fetch data and must expose a table fallback plus copy as CSV/JSON actions.
