# MCP Tools

Trees MCP tools call Trees APIs directly and emit `mcp.toolCalled` events.

Dangerous tools such as delete, ensure worktree, start agent, send agent, and stop agent require explicit capability enablement.
