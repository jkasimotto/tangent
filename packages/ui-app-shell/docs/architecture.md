# @tangent/ui-app-shell Architecture

The app shell renders a top bar, product switcher, repo selector, global search, command palette, package-specific sidebar, main route content, and optional inspector.

Product packages pass navigation models; the shell does not import product data.
