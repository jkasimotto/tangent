# Agent Notes

Purpose: Browser UI for Tangent Goals and Reviewed build Runs.

Local rules:
- Keep this package UI-only. Use `/api/work/*` for domain work.
- Do not import `@tangent/agent-shell`.
- Keep the default launch path to one action. Put overrides behind a detail control.

Read next:
- docs/index.md
- docs/architecture.md
- docs/public-api.md
