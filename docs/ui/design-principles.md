# UI Design Principles

Each screen has one primary job, visible next actions, contextual metrics, visible caveats, and progressive disclosure for raw metadata.

Tables and charts are paired. Raw JSON, provider fields, evidence ids, and long outputs belong in inspectors or disclosures by default.
