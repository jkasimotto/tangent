# UI

`tangent trees ui` starts Tangent Center.

The V1 layout prioritizes tree organization, attention items, active agents, selected entity context, and quick intervention. Raw terminal logs and provider metadata are inspector details, not the default surface.
