# @tangent/trees-core Public API

Import from `@tangent/trees-core`.

Public API includes `TreeEventStore`, `TreesClient`, `createTreesClient`, `createMemoryTreeEventStore`, `rebuildTreesProjection`, `resolveEntityRef`, and lifecycle helpers for entities, sessions, captures, observations, and attention.
