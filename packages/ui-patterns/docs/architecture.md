# @tangent/ui-patterns Architecture

Patterns compose primitives and product components into reusable workflows. They encode layout, hierarchy, progressive disclosure, and recovery surfaces without importing product domains.

Pattern documentation should include problem, use cases, anatomy, states, keyboard behavior, accessibility notes, content guidance, data requirements, analytics events, examples, anti-patterns, and acceptance criteria.
