# @tangent/eval-ui

Purpose: React screens for Eval run inspection and variant comparison.

Read next:
- docs/index.md

Local rules:
- Consume `@tangent/eval-ui-data`; do not remap raw server DTOs in components.
- Use shared Tangent UI packages before adding local components.
