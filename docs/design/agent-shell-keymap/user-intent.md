# Agent Shell keymap: user intent

Date: 2026-08-28

This note keeps Julian's request in his words. It extends `../agent-shell-navigation-model/user-intent.md` and `../agent-shell-work-search/user-intent.md`.

## Julian's words, condensed

- "Delete the capture note shortcut. I don't care about it."
- "Design all keybindings so vim keybindings are kind of central. Navigation ones that is."
- "Focus lets me filter the table, that is fine."
- "You can delete the search bar I think."
