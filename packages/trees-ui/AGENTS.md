# @tangent/trees-ui

Purpose: Svelte app for Tangent Trees browser surfaces.

Read next:
- docs/index.md

Local rules:
- Do not import old `pa` code or sidecar concepts.
- Keep Trees domain mapping outside generic UI platform packages.
