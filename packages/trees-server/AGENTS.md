# @tangent/trees-server

Purpose: local UI server adapter for Tangent Trees.

Read next:
- docs/index.md

Local rules:
- Keep server code framework-agnostic.
- Do not import old `pa` code or sidecar concepts.
