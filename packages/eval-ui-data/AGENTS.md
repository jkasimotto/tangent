# @tangent/eval-ui-data

Purpose: Eval UI DTOs and data clients, with no React dependency.

Read next:
- docs/index.md

Local rules:
- Keep React out of this package.
- Product components consume these DTOs instead of remapping raw API data.
