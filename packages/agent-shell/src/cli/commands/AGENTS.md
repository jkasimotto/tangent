# Agent Notes

Purpose: one file per CLI noun: `area.ts`, `goal.ts` (including `goal start` and `goal handover`), `idea.ts`, `agent.ts`, `vault.ts`.

Local rules: Follow `../AGENTS.md`. `goal done`/`goal wont-do` run only on the user's explicit word.

Read next:
- Package docs/index.md: ../../../docs/index.md
- ../../../docs/public-api.md
