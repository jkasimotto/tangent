# @tangent/core Architecture

Pure shared primitives for CLI specs, args, config, hashes, filesystem checks, and time.

Rules:
- Do not shell out.
- Do not write provider config.
- Do not import product domains.

Refer to ../../../docs/architecture/package-boundaries.md and ../../../docs/architecture/dependency-graph.md for monorepo boundaries.
