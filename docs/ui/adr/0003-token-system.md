# ADR 0003: Token System

Decision: centralize semantic colors, typography, spacing, radius, shadows, motion, z-index, and density in `@tangent/ui-tokens`.

Reason: product-specific hardcoded CSS variables made the eval prototype hard to reuse.
