{
  "name": "agent-shell-prototype",
  "private": true,
  "version": "0.0.1",
  "description": "Prototype: chat-first agent shell with embedded tmux terminals (xterm.js + node-pty)",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "app": "bash native/build-app.sh",
    "test": "node --test *.test.mjs",
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/darwin-*/spawn-helper || true"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
