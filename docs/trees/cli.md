# CLI

Primary namespace:

```bash
tangent trees ...
```

The CLI supports entity, project, worktree, agent, terminal, attention, session, capture, UI, events, import, and MCP commands. Mutations write `TreeEvent`s and commands that support machine output use `--json`.
