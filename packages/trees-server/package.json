{
  "name": "@tangent/trees-server",
  "version": "0.1.0",
  "description": "Local UI server adapter for Tangent Trees.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @tangent/ui-server && npm run build -w @tangent/trees-ui && npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@tangent/trees-ui": "^0.1.0",
    "@tangent/ui-server": "^0.1.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
