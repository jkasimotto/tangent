{
  "name": "@tangent/agent-shell",
  "version": "0.1.0",
  "description": "Vault, agent messaging, and pipeline CLI for the Tangent Agent Shell.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./cli": {
      "types": "./dist/cli/index.d.ts",
      "import": "./dist/cli/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @tangent/core && npm run build -w @tangent/agent-runtime && npm run build -w @tangent/repo && npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@tangent/agent-runtime": "^0.1.0",
    "@tangent/core": "^0.1.0",
    "@tangent/repo": "^0.1.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
