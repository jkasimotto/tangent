import assert from "node:assert/strict"; import test from "node:test"; import { createAreaMapProposals } from "./area-map-proposals.mjs";
const memory = () => { let value; return { async read(_a, _n, fallback) { return value ?? structuredClone(fallback); }, async write(_a, _n, next) { value = structuredClone(next); } }; };
test("keeps identity, versions updates, and fences stale decisions", async () => { const proposals = createAreaMapProposals({ store: memory(), id: () => "p" }); const first = await proposals.propose("otto", { kind: "file", source: { file: "otto/a.md" }, note: "A" }, {}); const second = await proposals.propose("otto", { kind: "file", source: { file: "otto/a.md" }, note: "B" }, {}); assert.equal(second.proposal.id, first.proposal.id); assert.equal(second.proposal.version, 2); assert.equal((await proposals.decide("otto", "p", 1, "dismissed")).status, 409); });
