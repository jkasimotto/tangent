# @tangent/trees-git

Purpose: Git project registry and worktree orchestration for Tangent Trees.

Rules:
- Reuse `@tangent/repo` helpers instead of copying Git/worktree logic.
- Keep Git behavior outside `trees-core`.

Read next:
- docs/index.md
