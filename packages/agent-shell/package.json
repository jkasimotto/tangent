{
  "name": "@tangent/agent-shell",
  "version": "0.1.0",
  "description": "Goal-bound programs and durable multi-agent runs for Tangent.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @tangent/agent-runtime && npm run build -w @tangent/repo && npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@tangent/agent-runtime": "^0.1.0",
    "@tangent/repo": "^0.1.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
