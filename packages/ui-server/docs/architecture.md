# @tangent/ui-server Architecture

The server accepts compiled UI assets and API route handlers. Product packages own their API routes; this package owns static serving, health, errors, and optional browser opening.
