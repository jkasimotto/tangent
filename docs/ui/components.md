# Components

Use `@tangent/ui-primitives` for controls, `@tangent/ui-components` for generic product components, and `@tangent/ui-patterns` for repeated workflows.

New product UI should check the inventory before adding local controls.
